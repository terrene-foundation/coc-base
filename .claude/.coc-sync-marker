# COC Sync Marker — coc-base (base lane, multi-CLI)
timestamp: 2026-07-02T15:55:24Z
source: loom/.claude
loom_sha: 618e23d
variant: base
clis: [claude, codex, gemini]
visibility: public
scope_this_cycle: cli-emission-only   # coc-sync Steps 6.5/6.6/6.7 — AGENTS.md/GEMINI.md/.codex/.gemini/.coc; NO .claude/ content re-distribution
stats:
  # Content axis (.claude/) was distributed in the PRIOR cycle (VERSION 2.45.7,
  # loom_sha 618e23d) and is unchanged here. Step 1 sync-tier-aware re-run was an
  # idempotent CONSISTENT re-write whose purpose was the presync untracked-snapshot
  # protecting the emit-cli writes (coc-sync Step 6.6 snapshot-ordering MUST).
  content_axis: unchanged   # 1159/1159 verified CONSISTENT (idempotent re-run)
  baselines_emitted:
    cc: 0          # CLAUDE.md preserved, not emitted
    codex: 1       # AGENTS.md (repo root)
    gemini: 1      # GEMINI.md (repo root)
  baseline_emission_bytes:
    block_cap_bytes: 61440
    warn_cap_bytes:  32768
    codex:
      rules: 10
      bytes: 52084
      tier: WARN
      headroom_bytes: 9356
      headroom_pct: 15.23
      headroom_floor_pct: 10
      headroom_floor_violations: 0
    gemini:
      rules: 10
      bytes: 52090
      tier: WARN
      headroom_bytes: 9350
      headroom_pct: 15.22
      headroom_floor_pct: 10
      headroom_floor_violations: 0
  cli_artifacts:      # from emit-cli-artifacts.mjs (Step 6.6)
    codex:
      prompts: 33     # 50 files (prompts + agent-prompts sub-tree)
      skills: 21      # 102 files
      agent_prompts: 17
    gemini:
      commands: 33
      skills: 21      # 102 files
      agents: 17
  coc_unified:        # from emit-coc.mjs (Step 6.7)
    artifacts: 135    # rules=54 agents=21 skills=26 commands=34
    files: 137
    coc_version: 1.0.0
  mcp_guard:
    files: 9
    policies_populated: false   # still runtime-gated per coc-sync Step 6.6 § guard
  codex_dispatcher:
    bin_coc: 1
    per_phase_symlinks: 28
  cli_trees_status: CURRENT@618e23d   # was STALE_RELATIVE_TO_e2ac475
  preserved: [CLAUDE.md, dev-container-scaffold, settings.local.json]
note: |
  CORRECTIVE multi-CLI emission (coc-sync Steps 6.5/6.6/6.7) for the base lane.
  The prior /sync-to-use base cycle (VERSION 2.45.7, loom_sha 618e23d) distributed
  the .claude/ CONTENT axis for the #743 delta but did NOT run the CLI-emission
  steps — leaving AGENTS.md / GEMINI.md / .codex/ / .gemini/ / .coc/ at the older
  e2ac475 emission (STALE_RELATIVE_TO_e2ac475). This cycle refreshes them.
  ORDER (snapshot-ordering MUST): (1) sync-tier-aware --target base --template
  coc-base = idempotent CONSISTENT re-run whose purpose was the presync untracked
  snapshot protecting the emit-cli writes; (2) emit.mjs --all (NO --lang → base
  variant, priority:0 baseline, no kailash-tier leak) → AGENTS.md/GEMINI.md, exit 0
  WARN tier (52084/52090 B, 15.23%/15.22% headroom, 0 floor violations);
  (3) emit-cli-artifacts.mjs --target base → .codex/{prompts,skills} +
  .gemini/{commands,skills,agents} + config + .codex-mcp-guard/ + bin/coc + 28
  per-phase symlinks, exit 0; (4) emit-coc.mjs --target base → .coc/ (137 files),
  exit 0 with 1 surfaced advisory (rules/SELF-REFERENTIAL-CODIFY.md 87907B over the
  60 KiB PRODUCER budget — spec-09 imposes NO consumer cap, emitted-not-truncated
  by design; not a failure).
  PRESERVED: CLAUDE.md (template-owned), dev-container scaffold, settings.local.json.
  visibility=public preserved across marker rewrite.
  build:null — base is stack-agnostic, NO SDK pins.
