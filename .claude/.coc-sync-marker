# COC Sync Marker — coc-base (base lane, multi-CLI)
timestamp: 2026-06-26T23:00:53Z
source: loom/.claude
loom_sha: 9101e34
variant: base
clis: [claude, codex, gemini]
visibility: public
stats:
  copied: 0            # narrow propagation — no .claude/ content re-emit (--verify CONSISTENT)
  variant_only: 0
  overlay_applied: 0   # rules/agents.md base slot byte-equal from prior cycle (verify-overlays Failing:0)
  verify_failures: 0
  purged: 0            # codex-templates/+gemini-templates/ already ABSENT on base disk (base always clean)
  obsoleted_union: 69  # regenerated .coc-obsoleted (was 64 unique / 66 lines); +codex-templates/+gemini-templates/+sync.md trio, additive
  preserved: [CLAUDE.md, .proposals/, learning/, settings.local.json, dev-container-scaffold]
  baselines_emitted:
    cc: 0          # CLAUDE.md preserved, not emitted
    codex: 0       # AGENTS.md byte-equal from prior cycle — no re-emit this cycle
    gemini: 0      # GEMINI.md byte-equal from prior cycle — no re-emit this cycle
  baseline_emission:
    block_cap_bytes: 61440
    warn_cap_bytes: 32768
    headroom_floor_pct: 10
    codex: { tier: WARN, bytes: 52479, headroom_pct: 14.58 }
    gemini: { tier: WARN, bytes: 52485, headroom_pct: 14.58 }
  cli_artifacts:
    codex: { prompts: 50, skills: 22 }      # prompts = 33 commands + 17 agent-prompts
    gemini: { commands: 33, skills: 22, agents: 17 }
  cli_orphans_purged: { codex_prompts: 0, codex_skills: 0, gemini_commands: 0, gemini_skills: 0, gemini_agents: 0 }
  rules_reference_skill: { codex: 1, gemini: 1, path_scoped_rules_indexed: 44 }
  codex_dispatcher: { bin_coc: 1, phase_shims: 28 }
  coc_unified: { artifacts: 79, files: 82, version: "1.0.0" }
  mcp_guard: { policies_populated: false }
note: |
  loom@9101e34 narrow propagation (base multi-CLI, loom 2.45.0 unchanged — 9755c2d..9101e34).
  HEADLINE: PR #685 added .claude/codex-templates/ + .claude/gemini-templates/ to
  sync-manifest::obsoleted (loom-internal Codex/Gemini emitter SOURCE trees; an
  F1-distribution-redteam MED finding on the rs templates where a v2.31.0 catch-up had committed
  them — base templates were ALWAYS clean). This is a manifest-propagation cycle, NOT a content
  re-emit and NOT a multi-CLI re-emit. sync-tier-aware.mjs --target base --all-templates --verify
  reported CONSISTENT (1056/1056 in-plan, no obsoleted path present on disk). REQUIRED CHANGE:
  .coc-obsoleted regenerated (Step 4.5 union of obsoleted+use_obsoleted) — dedup union now 69
  path entries (was 64 unique / 66 lines), with .claude/codex-templates/ + .claude/gemini-templates/
  now present. ADDITIVE: 0 prior entries dropped (comm -23 empty); the prior file's internal
  duplicate terrene-naming/independence pair collapsed to single entries; +sync.md trio folded in
  (already manifest-declared). Both template dirs absent on disk → 0 purged here; the .coc-obsoleted
  entry keeps downstream /sync-from-template consumers able to purge them.
  NO MULTI-CLI RE-EMIT: AGENTS.md + GEMINI.md + .codex/** + .gemini/** + .codex-mcp-guard/** +
  bin/coc + .coc/** unchanged this cycle (9755c2d..9101e34 delta is the 13-line manifest obsoleted
  addition only — no rule/agent/skill/command body reaches any CLI surface). Prior-cycle emission
  retained: .codex/prompts=50, .codex/skills=22, .gemini/commands=33, .gemini/skills=22,
  .gemini/agents=17; AGENTS.md/GEMINI.md both WARN 14.58% headroom; bin/coc + 28 phase shims;
  .coc/ (79 artifacts, 82 files); .codex-mcp-guard POLICIES_POPULATED false by construction.
  SCANS: 0 operator-PII, 0 scripts/hooks refs, 0 loom-internal path leaks, 0 Kailash coc-tier
  leaks. verify-overlays.sh base Failing:0. build:null — no SDK pins (base stack-agnostic; no pin
  change). VERSION 2.45.4->2.45.5; loom_sha 9755c2d->9101e34; upstream loom 2.45.0 unchanged.
