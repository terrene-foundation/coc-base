# COC Sync Marker — coc-base (base lane, multi-CLI)
timestamp: 2026-08-17T10:45:00Z
source: loom/.claude
loom_sha: f3513659
variant: base
clis: [claude, codex, gemini]
visibility: public
scope_this_cycle: content+cli-emission   # FULL cycle: .claude/ content axis AND coc-sync Steps 6.5/6.6/6.7
stats:
  engine_changed: 266   # engine file(s) applied; base origin/main fe4d10c8cb6d
  verify_failures: 0    # stage engine sync-tier-aware --verify CONSISTENT (2304 in-plan files)
  version_stamp: "loom-tracking -> 2.45.0 @ f3513659"
  baselines_emitted: { cc: 0, codex: 1, gemini: 1 }   # AGENTS.md + GEMINI.md 57801B each, WARN tier, headroom 11.8%
  cli_artifacts:
    codex: { prompts: 39, agent_prompts: 18, skills: 28 }
    gemini: { commands: 39, skills: 28, agents: 18 }
  coc_artifacts: 167   # 169 files; rules=72 agents=21 skills=33 commands=41
sdk_pins: "N/A (build:null — non-Kailash base). Steps 9-10 NOT APPLICABLE, not skipped."
codex_mcp_guard: "FROZEN — .codex-mcp-guard/** deliberately excluded (continuing the prior security-review deferral); the emit-written scratch `codex-mcp-guard/` was REMOVED before commit; symlink .claude/codex-mcp-guard -> ../.codex-mcp-guard preserved + verified resolving (policies.json reachable)"
preserved: [CLAUDE.md, settings.json, "visibility:public", ".codex-mcp-guard/** (frozen)"]
note: |
  loom@43167a54->f3513659 Gate-2 base sync (multi-CLI lane) — the xref-integrity
  release. This is the FIRST Gate-2 delivery from a corpus that passes loom's own
  cross-reference gate, which loom had been shipping to every consumer as a
  REQUIRED check while never running it on itself.

  WHAT THIS CORPUS FIXES, measured at the consumer rather than asserted:

    dangling cross-references at coc-base:  374  ->  1
    the consumer's own declared BASELINE:   339

  The previous Gate-2 attempt (cut at 74f60056) measured 374 against that 339 and
  correctly RED — "rose from 339 to 374". This corpus measures 1, and the single
  residual is a `--out` materialization artifact (`../../../CLAUDE.md` from a
  skill), not a defect: root CLAUDE.md comes from the enrichment step, verified
  present in this repo at 9957B with a firing 404 control on a bogus sibling path.

  ACTION FOR THIS REPO: `BASELINE=339` in .github/workflows/validate.yml is now
  334 higher than the true count. It should be ratcheted down, or a future
  regression back up to 339 passes silently. Left for a separate change rather
  than bundled here.

  DERIVED TREES RE-EMITTED, and the placement verified rather than assumed:
  emit.mjs --cli codex|gemini (AGENTS.md + GEMINI.md), emit-cli-artifacts.mjs
  --target base (.codex/ + .gemini/), emit-coc.mjs --target base --lane use
  (.coc/). emit.mjs nests its output per-CLI under <out>/<cli>/ with no root
  option, so the ROOT baselines were placed explicitly and confirmed byte-equal to
  the freshly emitted pair; before that step the tracked root copies were still the
  PREVIOUS cycle's 54143B files and `git status` reported them unmodified. The
  emit-written scratch (`codex/`, `gemini/`, `codex-mcp-guard/`,
  `emit-telemetry.json`) was removed before commit.

  DISCLOSURE: scan CLEAN on the loom SOURCE tree (4330 files) and CLEAN on this
  enriched worktree (2614 files), both 0 findings. The enriched scan was run
  with a planted-token control that FIRED (3 hits) and cleared on restore, so the
  clean is discriminating rather than vacuous. Scope caveat recorded honestly: the
  scanner covers root baselines and `.codex/**` (controls fired) but NOT `.coc/**`
  (control did not fire). `.coc/**` is a projection of the same loom corpus that
  scanned clean at source, so no new exposure is implied — but the fence does not
  cover it, and that gap is filed upstream rather than papered over.

  PROVENANCE: --finalize REFUSED this delivery once, correctly, because the stamp
  still named loom_sha=43167a54 over content it did not deliver. Restamped to
  f3513659 before re-running. visibility=public preserved across marker rewrite.
